markdown
# تدفق المصادقة - Authentication Flow

## 📊 مخطط التدفق
User Registration → Create User (is_verified=false) → Send Verification Code →
Verify Page → Enter Code → Activate Account → Auto Login → Dashboard

text

## 🔐 آلية الأمان
- **التسجيل**: لا ينشئ توكن - ينتظر التحقق
- **التحقق**: كود مؤقت (15 دقيقة) عبر البريد
- **الدخول**: ينشئ توكن JHT بعد التحقق فقط
- **الجلسات**: توكن صالح مع تحقق من الصلاحية

## 🗂️ جداول قاعدة البيانات
- **users**: تخزين بيانات المستخدمين وأكواد التحقق
- **customers**: تخزين بيانات العملاء
- **licenses**: إدارة التراخيص

## 🔧 نقاط نهاية API
- `POST /api/auth/register` - تسجيل جديد
- `POST /api/auth/verify` - تحقق من الكود
- `POST /api/auth/login` - دخول مستخدم
- `POST /api/auth/resend` - إعادة إرسال الكود
